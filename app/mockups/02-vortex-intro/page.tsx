import { Homepage } from '@/features/homepage/Homepage';

export const metadata = { title: 'Mockup 02 — Particle Vortex Intro' };

/** PRESERVED CONCEPT. The four-act vortex opening in isolation — gateway, expansion, nebula
 *  reveal, hero birth — without the capability fragments. */
export default function Mockup02() {
  return <Homepage variant="vortex-intro" />;
}
