import { Homepage } from '@/features/homepage/Homepage';

export const metadata = { title: 'Mockup 03 — Technology Fragment Journey' };

/** PRESERVED CONCEPT. The full journey: vortex gateway into the nebula, then the capability
 *  fragments with their floating holographic panels. Same as the live route. */
export default function Mockup03() {
  return <Homepage variant="fragment-journey" />;
}
