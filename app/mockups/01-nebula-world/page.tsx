import { Homepage } from '@/features/homepage/Homepage';

export const metadata = { title: 'Mockup 01 — Original Nebula World' };

/** PRESERVED CONCEPT. The nebula world fully revealed from the first frame: no vortex gate,
 *  no capability fragments. Kept so the original environment stays comparable. */
export default function Mockup01() {
  return <Homepage variant="nebula-world" />;
}
